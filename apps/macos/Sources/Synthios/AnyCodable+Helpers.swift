import Foundation
import SynthiosKit

// Prefer the SynthiosKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = SynthiosKit.AnyCodable
typealias InstanceIdentity = SynthiosKit.InstanceIdentity
