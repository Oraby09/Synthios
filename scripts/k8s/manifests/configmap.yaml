apiVersion: v1
kind: ConfigMap
metadata:
  name: synthios-config
  labels:
    app: synthios
data:
  synthios.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.synthios/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Synthios Assistant",
            "workspace": "~/.synthios/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Synthios Assistant

    You are a helpful AI assistant running in Kubernetes.
