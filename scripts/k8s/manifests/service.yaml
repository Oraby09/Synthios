apiVersion: v1
kind: Service
metadata:
  name: synthios
  labels:
    app: synthios
spec:
  type: ClusterIP
  selector:
    app: synthios
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
